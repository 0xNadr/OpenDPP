# OpenDPP environment variables
# Copy this file to .env and adjust for your environment.

# SQLAlchemy async DSN for Postgres.
# Default matches the docker-compose service defined at the repo root.
DATABASE_URL=postgresql+asyncpg://opendpp:opendpp@localhost:5432/opendpp

# Enable FastAPI debug mode (verbose errors, auto-reload).
DEBUG=false
